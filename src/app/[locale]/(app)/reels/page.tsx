'use client';

import FeedReels from '@/components/feed/FeedReels';

/**
 * Pagina Reels — Feed verticale stile TikTok.
 * Contenuti video e immagini swipabili in verticale con autoplay.
 */
export default function ReelsPage() {
  return <FeedReels />;
}
